# verify-5 prompt — Spec-30 Round-1 Bug Hunt, Phase 5 (Settings & Providers Hunt, US5)

> **How to use (Pilot)**: Open a SEPARATE orchestrator session (regular Claude Code — NOT the hunt Lead pane, NOT `/sdd-apply`). Paste everything below the line. The orchestrator delegates an `sdd-verify` sub-agent (model: sonnet) to run the R-002 gate on Phase 5, then adjudicates.

---

Run the **verify-5 gate** for the SDD change **`single-round-bug-hunt`**, Phase 5 (Settings & Providers Hunt, US5), by delegating an **`sdd-verify` sub-agent** (model: sonnet). This is the SDD **R-002** verify gate (task **T-014**) between apply-5 and apply-6. Do NOT type `/sdd-apply`.

**IMPORTANT — this change is ENGRAM-backed** (backend: engram, project `the-embedinator`), NOT OpenSpec. The native SDD dispatcher reads only OpenSpec files and is BLIND to this change — do NOT rely on it; delegate `sdd-verify` directly with the engram references below.

**State to verify:**
- Phase 5 was CLOSED at commit `2c4e9d2` ("chore(spec-30-r1): close hunt phase 5 — 13 findings (BUG-089..101)") on branch `030-e2e-test-v3`. It is a **single clean commit** (no WIP chain, unlike Phase 4).
- Registry: 78 bugs (BUG-024..101), next-bug-id `BUG-102`. Phase-5 contributed **13 NEW findings** (BUG-089..101).
- Self-reported census of the 13 NEW: **3 CRITICAL** (BUG-089 cloud-providers-unconfigurable, BUG-095 settings-page-write-only, BUG-098 no-embedder/collection-compatibility-check) / **2 MAJOR** (BUG-097 citation-renderer-block-wraps-fragments/Frontend, BUG-099 circuit-breaker-counts-client-errors) / **7 MINOR** (BUG-090, 091, 092, 094, 096, 100, 101) / **1 COSMETIC** (BUG-093 provider-card-renders-"Openai" CSS-capitalize/Frontend).
- **1 RE-ADJUDICATION** (no new ID): BUG-070 MINOR/Observability → **MAJOR/Backend** — its "computed reference text never reaches the browser" mitigation claim RETRACTED (falsified on the zero-retrieval path: `fallback_response` bypasses `collect_answer` streaming, so `has_streamed_response=False` emits `format_response`'s raw **References:** block to the client). Blast radius = infra-failure tail only (`research_edges.py` gates fallback on zero `retrieved_chunks`).
- **8 UPDATEs** (no new IDs): BUG-024, 047, 061, 066, 078, 079, 080, 081.
- **5 scenarios P5-S1..S5 all closed** (Phase 5 has 5 scenarios, NOT 6): S1 **SPLIT** (UI-affordance FAIL=BUG-089 CRITICAL / encryption **ENGINEERED PASS**), S2 **SPLIT** (persistence PASS byte-identical ciphertext across restart / decryption testable only out-of-band via `/api/providers/health`, exposed BUG-094), S3 **FAIL** (Settings write-only=BUG-095 CRITICAL), S4 **FAIL** (embedder/collection mismatch=BUG-098 CRITICAL; 50 phantom citations for 0 retrievals), S5 **PASS** (SC-012 plaintext-key audit, ENGINEERED — the first & only engineered security pass of the hunt, re-audited post-restart/S3/S4 churn).
- Registry-wide severity mix after Phase 5 (self-reported): **1 BLOCKER / 8 CRITICAL / 30 MAJOR / 34 MINOR / 5 COSMETIC = 78**.

**The `sdd-verify` sub-agent MUST (reads-only; it writes only the verify report):**

1. Read the SDD spec for the R-002 gate criteria: engram `mem_search(query: "sdd/single-round-bug-hunt/spec", project: "the-embedinator")` → `mem_get_observation` (#3218). Apply the R-002 gate checks (a/b/c/d) to Phase 5.
2. Read the prior verify reports for format + precedent, and follow the same report structure and adjudication style:
   - verify-1: #3444 / decision #3445 — PASS
   - verify-2: report #3500 / decision #3502 — PASS
   - verify-3: report #3807 / decision #3808 — PASS (0 CRITICAL / 1 WARNING / 4 SUGGESTION)
   - verify-4: report #3859 / decision #3860 — PASS (0 CRITICAL / 0 WARNING / 2 SUGGESTION)
3. Read the apply-progress checkpoint: `sdd/single-round-bug-hunt/apply-progress` (#3227) — confirm the **phase-5** checkpoint (commit `2c4e9d2`, 13 findings, next-bug-id BUG-102) and that phases 0–4 are preserved verbatim unmodified (merge discipline, no overwrite).
4. Validate the Phase-5 registry on disk under `docs/E2E/2026-05-28-round-1-bug-hunt/`:
   - The 13 NEW findings **BUG-089..101** are each schema-complete against `specs/030-e2e-test-v3/contracts/bug-registry-schema.json`: `id` pattern, `severity` + `layer` valid enums, `phase`/`scenario_id`/`discovered_at` present, ≥1 reproduction step, ≥1 NON-NULL artifact path, `title` ≤80 chars.
   - **PRIORITY — the 3 CRITICAL (BUG-089, 095, 098) and 2 MAJOR (097, 099) must have every required field**, each with a HIGH-confidence, code-confirmed root cause citing file:line.
   - **TITLE MEASUREMENT RULE (known false-positive trap)**: the schema (`$defs.bug.properties`) stores `id` and `title` as SEPARATE fields. Measure the `title` field WITHOUT the `BUG-0NN: ` prefix, and NOT off the filename (filenames carry slugs like `BUG-089-cloud-providers-unconfigurable-no-create-path.md`). Measuring off the H1 or filename manufactures a phantom overflow.
   - Independently re-derive the severity census from the 13 files on disk (NOT the self-report). Confirm **78 bug files total** (BUG-024..101) and `next-bug-id.txt` = `BUG-102`.
   - Registry-wide severity re-count by grepping all 78 files: compare to the claimed **1 BLOCKER / 8 CRITICAL / 30 MAJOR / 34 MINOR / 5 COSMETIC = 78**.
   - **RE-ADJUDICATION coherence spot-check (Phase-5-specific, PRIORITY — the analog of verify-4's BUG-086 check)**: BUG-070 was bumped MINOR/Observability → MAJOR/Backend precisely because its "computed reference text never reaches the browser" mitigation was RETRACTED (falsified on the zero-retrieval path). Confirm BOTH: (a) the file's `Severity: MAJOR` / `Layer: Backend` header fields are updated, AND (b) the file BODY coherently reflects the retraction — i.e. a Notes entry explaining the falsification (fallback path bypasses streaming). **If the body still asserts the old "never reaches the browser" mitigation as an unqualified claim while the severity is now MAJOR because that claim is false, raise it as a finding** (internal incoherence — grade WARNING or SUGGESTION per how load-bearing the stale text is; NOT CRITICAL). Read the full file before judging; verify-4 taught that a claim that reads as incoherent on a grep is often precise in full context.
   - **BUG-091 coherence**: registered MINOR but described as "armed by BUG-089's fix" (a latent/conditional plaintext-key path). Confirm the MINOR severity is coherent with a not-yet-live defect, or flag it.
   - `session-log.md` contains the Phase-5 phase-transition entries: **OPEN** (`[2026-07-10T10:58:34-03:00] … Phase 5 … OPEN`) and **CLOSED** (`[2026-07-10T12:55:21-03:00] … Phase 5 … CLOSED → Phase 6 … ready`), plus per-scenario verdicts **P5-S1..S5**. NOTE: session-log lines are multi-thousand-char single lines — a token/line-capped read terminates early while appearing complete (METHODOLOGY TRAP #6, recorded in the OPEN entry). Verify against `tail -1` / explicit line numbers, not a truncated read.
   - Coverage: all **5** scenarios exercised, each with an explicit verdict. S1/S2 are **SPLIT** verdicts (part PASS / part FAIL) — confirm the split framing is coherent. **P5-S1's UI-affordance path was salvaged via an out-of-band SQLite seed** (`INSERT INTO providers … 'openai' … is_active=0`), documented at session-log `[2026-07-10T11:41:04-03:00]` as **test-fixture data seeding — NOT a fix, NOT a workaround, NOT a production code edit** (the unreachability IS BUG-089 CRITICAL, already registered; the seed only unblocks the *encryption* assertion, S1's UI verdict stays FAIL). Confirm this is coherently framed and does NOT constitute an SC-006 production-code violation (it is application DATA, invisible to `git diff`).
   - FR-014/FR-015: no unfiled CRITICAL/BLOCKER; no untriaged MAJOR. Per verify-1 ADJUDICATION-1 and the verify-2/3/4 precedent, these are **hunt-close (Phase-8)** requirements — `Triage (filled in Phase 8 for MAJOR+)` TBD placeholders remain acceptable at Phase-5 scope. Do not re-litigate.
5. **SC-012 phase-local key-audit (T-014 additional check, REQUIRED for verify-5)**: confirm the SC-012 plaintext-key-audit log entry is present for Phase 5 and that the audit was **RE-RUN after state changed**, not asserted once. Evidence chain: session-log `[2026-07-10T11:51:56-03:00]` marks P5-S5 **PROVISIONALLY PASSED at P5-S1 scope** with 8 named guards + all-surfaces-zero-plaintext audit (including a first-hand read of Docker's raw on-disk container JSON log inside a root container: `sk-test`=0, `PLACEHOLDER`=0, `gAAAAA`=0 on 5,735,497 bytes); the Phase-5 CLOSE entry `[2026-07-10T12:55:21-03:00]` confirms P5-S5 PASS "re-audited post-churn". Confirm the re-audit actually happened (S2 restart exercises decrypt-at-init; S3/S4 could add logged surfaces) — a PASS never re-tested after state changed is not a PASS.
6. **R-005-A write boundary (bug-registrar sole writer)**: every Phase-5 new-bug-minting event in `session-log.md` is tagged `BugRegistrar | discovery`, never `Orchestrator | observation … REGISTERED`. **Known false-positive trap**: a case-insensitive `REGISTERED` grep matches "not registered" / "before registration" in deferral or narrative lines — read the surrounding context; those are pre-registration narrative, not minting events. verify-4 confirmed the tagging discipline held for all 12 registrations; confirm it held again for all 13 this phase. Also confirm all Phase-5 commit(s) touch ONLY files under `docs/E2E/2026-05-28-round-1-bug-hunt/` (`git show --name-only 2c4e9d2`).
7. **SC-006 invariant (zero production code)** — run `git fetch origin develop:develop` FIRST, then diff against `origin/develop`, NOT local `develop` (verify-3's SUGGESTION-2: a stale local `develop` ref is a false positive here). Expected: **empty diff** over `backend/ frontend/ ingestion-worker/ Makefile embedinator.sh embedinator.ps1 'docker-compose*.yml'`. The out-of-band `openai` provider-row seed (step 4) is application DATA and does NOT appear in a git diff — it is NOT a production-code edit. Confirm `origin/develop` is an ancestor of HEAD.
8. Also run the structural checks: **R-001-A** (checkpoint fields present, prior 4 preserved), **R-003-B** (BUG-089..101 contiguous, no gaps/dupes; `next-bug-id.txt`=BUG-102 = 101+1), **R-004-A/B** (`docs/E2E/2026-05-28-round-1-bug-hunt/process/lead-prompt.md` ≤300 lines, `grep -ci speckit`=0), **dormant-dir guard** (`git log --oneline a5e0886..HEAD -- "docs/E2E/2026-05-15-round-1-bug-hunt/"` must be EMPTY — scope it to `a5e0886..HEAD`; an unscoped `git log` returns pre-apply-0 historical commits and is a false positive).

**KNOWN carry-forwards — do NOT fail the gate on these** (accepted, consistent with the verify-1/2/3/4 standard):
- **BUG-055..067 reference schema-valid but non-existent local artifact files** (registered pre-convention; artifacts gitignored per FR-022). Treat a non-null artifact PATH as satisfying the "non-null artifact" check. Flag as SUGGESTION (spec-31 backfill), not CRITICAL. Out of Phase-5 scope anyway.
- **Expected uncommitted working-tree state, NOT drift**: modifications to `.claude/skills/gitnexus/*/SKILL.md` (5 files); untracked `docs/superpowers/`; any post-close `session-log.md` observation line. None belong to the phase-5 close commit — do not read them as SC-006 drift.
- **CLEANUP items (carried to Pilot / demo-prep — do NOT action here, do NOT fail the gate)**: the out-of-band seeded `openai` provider row (is_active=0, config_json `{"model":"gpt-4o-mini"}`) is BUG-098's live reproduction, rollback `DELETE FROM providers WHERE name='openai'`; the Qdrant `poison-test-p4s5` collection is BUG-087's live reproduction — if recommending purge, it MUST be via `collections.py::delete_collection`, NEVER `DELETE /api/documents` (that path leaves Qdrant orphans, per BUG-087 itself); local `~/Downloads/poison-p4s5*.md`; the `nag-corpus-bge-m3` (1024-dim) collection created during P5-S4. **`nag-corpus-spec28` is confirmed intact for phases 6-7** — flag corpus integrity as a SUGGESTION for the Pilot to re-confirm before apply-6, not a gate blocker.

**Deliverable:** Produce a verify report with findings graded CRITICAL / WARNING / SUGGESTION, plus a **PASS / FAIL / CONDITIONAL-PASS** verdict on the R-002 gate, a Summary Table (one row per check), and an explicit carry-forwards section (preserve verify-4's 7 carry-forwards, updating any that Phase 5 resolved or extended — notably the **verify-7 HARD GitHub-issue-URL debt** now covers **1 BLOCKER + 8 CRITICAL + 30 MAJOR = 39 bugs**, up from 33 at verify-4). Save it to engram topic `sdd/single-round-bug-hunt/verify-report-phase-5` (type: `architecture`, project: `the-embedinator`, `capture_prompt: false`). **Then also RETURN a concise Result Contract as your final message** (status, verdict, CRITICAL/WARNING/SUGGESTION counts, the verify-report topic key) — do NOT end on an idle notification; the orchestrator gates on the returned summary AND independently re-pulls the artifact.

**Then adjudicate (orchestrator):**
- **PASS or CONDITIONAL-PASS with only WARNING/SUGGESTION** → apply-6 is UNBLOCKED. Record the decision to engram topic `sdd/single-round-bug-hunt/verify-gate-5-decision`.
- **Any CRITICAL** → report the blocker and STOP; do NOT advance to apply-6.

Report back: the verdict, the count of CRITICAL/WARNING/SUGGESTION, and the verify-report topic key.
