# verify-6 prompt — Spec-30 Round-1 Bug Hunt, Phase 6 (Observability Hunt, US6)

> **How to use (Pilot)**: Open a SEPARATE orchestrator session (regular Claude Code — NOT the hunt Lead pane, NOT `/sdd-apply`). Paste everything below the line. The orchestrator delegates an `sdd-verify` sub-agent (model: sonnet) to run the R-002 gate on Phase 6, then adjudicates — with **BUG-107 by-design-vs-regression** as the priority adjudication.

---

Run the **verify-6 gate** for the SDD change **`single-round-bug-hunt`**, Phase 6 (Observability Hunt, US6), by delegating an **`sdd-verify` sub-agent** (model: sonnet). This is the SDD **R-002** verify gate between apply-6 and apply-7. Do NOT type `/sdd-apply`.

**IMPORTANT — this change is ENGRAM-backed** (backend: engram, project `the-embedinator`), NOT OpenSpec. The native SDD dispatcher reads only OpenSpec files and is BLIND to this change — do NOT rely on it; delegate `sdd-verify` directly with the engram references below.

**PATH CORRECTION vs prior verify prompts**: the bug files live in the **`bugs/` subdirectory**: `docs/E2E/2026-05-28-round-1-bug-hunt/bugs/BUG-NNN-*.md`. `next-bug-id.txt` and `session-log.md` are in the parent `docs/E2E/2026-05-28-round-1-bug-hunt/`. Other subdirs: `logs/ screenshots/ traces/ public-evidence/` (artifacts, gitignored).

**State to verify:**
- Phase 6 was CLOSED at commit `e9425f9` ("chore(spec-30-r1): close hunt phase 6 — 16 findings (BUG-102..117)") on branch `030-e2e-test-v3`. It is a **single clean commit** (no WIP chain).
- Registry: 94 bugs (BUG-024..117), next-bug-id `BUG-118`. Phase-6 contributed **16 NEW findings** (BUG-102..117).
- Self-reported census of the 16 NEW: **6 MAJOR** (BUG-102 stage-timings-chart-drops-dominant-stage, BUG-103 ranking-stage-always-zero-dead-node, BUG-107 validate_citations-100pct-noop **[FLAGGED]**, BUG-108 rewrite_query-no-stage-timing-hidden-latency, BUG-109 ingest-error-dead-end-ui-drops-ids, BUG-112 analytics-distributions-from-20-row-page) / **9 MINOR** (BUG-104, 105, 110, 111, 113, 114, 115, 116, 117) / **1 COSMETIC** (BUG-106 trace-stage-bars-one-color-map-mismatch).
- **1 CORRECTION** (no new ID): **BUG-110** reframed from failure-path-specific ("failed ingest job never sets completed_at") → **universal** (ALL 79 `ingestion_jobs` rows have `finished_at=NULL`, 78 completed + 1 failed). Title/Actual/Steps/Root-cause were **overwritten** (not just a Note appended), severity unchanged (MINOR/Backend/P6-S2).
- **4 UPDATEs** (no new IDs): BUG-042, 047, 048, 051.
- **4 scenarios P6-S1..S4 all exercised, all FAIL** (Phase 6 has 4 scenarios, NOT 5 or 6): S1 trace-navigation-post-chat, S2 error-navigability, S3 performance-budget-chart, S4 log-surface-user-readability.
- Registry-wide severity mix after Phase 6 (self-reported): **1 BLOCKER / 8 CRITICAL / 36 MAJOR / 43 MINOR / 6 COSMETIC = 94**.
- **METHOD NOTE (new this phase)**: Phase 6 was driven **Lead-solo via chrome-devtools MCP** — the Pilot granted mid-phase consent for the Lead to operate the browser directly. `frontend-inspector` was **PASSIVE-ONLY** (source read only; the Lead held the CDP lock). `bug-registrar` remained the **sole writer** (R-005-A). Malformed-PDF ingest test used a throwaway collection `audit-timing-test` and left a real failed doc `31503bd4` (= BUG-115 live repro).

**The `sdd-verify` sub-agent MUST (reads-only; it writes only the verify report):**

1. Read the SDD spec for the R-002 gate criteria: engram `mem_search(query: "sdd/single-round-bug-hunt/spec", project: "the-embedinator")` → `mem_get_observation` (#3218). Apply the R-002 gate checks (a/b/c/d) to Phase 6.
2. Read the prior verify reports for format + precedent, and follow the same report structure and adjudication style:
   - verify-1: #3444 / decision #3445 — PASS
   - verify-2: report #3500 / decision #3502 — PASS
   - verify-3: report #3807 / decision #3808 — PASS (0 CRITICAL / 1 WARNING / 4 SUGGESTION)
   - verify-4: report #3859 / decision #3860 — PASS (0 CRITICAL / 0 WARNING / 2 SUGGESTION)
   - verify-5: report #3878 / decision #3879 — PASS (0 CRITICAL / 1 WARNING / 3 SUGGESTION)
3. Read the apply-progress checkpoint: `sdd/single-round-bug-hunt/apply-progress` (#3227) — confirm the **phase-6** checkpoint (commit `e9425f9`, 16 findings, next-bug-id BUG-118) and that phases 0–5 are preserved verbatim unmodified (merge discipline, no overwrite).

4. **PRIORITY ADJUDICATION — BUG-107 by-design-vs-regression (this gate's single most important task).** BUG-107 was registered MAJOR/Backend with an explicit `Notes` FLAG: "by-design-vs-regression UNCONFIRMED, flagged for verify-gate adjudication." Resolve it on **independently re-derived evidence**:
   - Read `docs/E2E/2026-05-28-round-1-bug-hunt/bugs/BUG-107-validate-citations-100pct-noop-reranker-unbound.md` in full, plus BUG-103 (same root cause, timing-symptom side).
   - Read the full `validate_citations` implementation in `backend/agent/nodes.py` (the early-return guard at ~`:651` `if not citations or not reranker:` and the alignment logic at ~`:671-712`), and the node registration at `backend/agent/conversation_graph.py:65`. Note the `# Phase 2 stubs` comment at `conversation_graph.py:61` groups `validate_citations` (:65) together with `verify_groundedness` (:62-64).
   - **DECISIVE EVIDENCE**: run git history on the binding site to determine whether a reranker was **EVER** bound and later removed (→ regression) or **never** bound (→ by-design/incomplete stub). Use e.g. `git log -p --follow -- backend/agent/conversation_graph.py` scoped to the `add_node("validate_citations"...)` line, and `git log -p -- backend/agent/nodes.py` for the `validate_citations` signature/guard. Check whether `build_conversation_graph` ever accepted or threaded a `reranker` argument.
   - Return a reasoned verdict — **by-design stub | wiring regression | genuinely-ambiguous** — with cited file:line + commit evidence. This RESOLVES the flag; it does NOT re-grade MAJOR unless the evidence demands it (a scaffolded-but-never-wired QA node is a legitimate MAJOR "advertised-but-inert" finding regardless of intent). If the evidence is by-design, the correct outcome is: keep the finding, replace the UNCONFIRMED flag framing with the adjudicated conclusion. Flag any remaining incoherence as WARNING/SUGGESTION, NOT CRITICAL.

5. **BUG-110 CORRECTION coherence (analog of verify-5's BUG-070 re-adjudication check, PRIORITY).** Read `bugs/BUG-110-failed-ingest-job-completed-at-never-set.md` in FULL. Confirm ALL of: (a) `Title`, `Actual`, `Steps to Reproduce`, and `Root-cause hypothesis` were **overwritten** to the universal (all-79-rows) framing — NOT merely a Note appended; (b) a `## Correction` section documents the falsification of the old failure-path-specific framing; (c) **no stale "failed-job-specific" assertion survives elsewhere in the body as an unqualified claim** while the record now asserts the universal framing (internal incoherence → WARNING/SUGGESTION per how load-bearing, NOT CRITICAL); (d) MINOR/Backend/P6-S2 is coherent with a universal-but-cosmetic-impact defect. Read the full file before judging — verify-4/5 taught that a claim that reads as incoherent on a grep is often precise in full context.

6. **BUG-103 ⇄ BUG-107 shared-root-cause coherence.** Both are attributed to the unbound-reranker root cause (BUG-103 = "ranking stage always 0" timing-symptom side; BUG-107 = the no-op itself). Confirm they are coherently cross-referenced and do NOT double-count the same defect as two independent root causes (two distinct observable symptoms of one cause is acceptable and correct; a silent duplicate is not).

7. Validate the 16 NEW findings **BUG-102..117** on disk under `docs/E2E/2026-05-28-round-1-bug-hunt/bugs/`:
   - Each is schema-complete against `specs/030-e2e-test-v3/contracts/bug-registry-schema.json`: `id` pattern, `severity` + `layer` valid enums, `phase`/`scenario_id`/`discovered_at` present, ≥1 reproduction step, ≥1 NON-NULL artifact path, `title` ≤80 chars.
   - **PRIORITY — the 6 MAJOR (BUG-102, 103, 107, 108, 109, 112) must have every required field**, each with a HIGH-confidence, code-confirmed root cause citing file:line.
   - **TITLE MEASUREMENT RULE (known false-positive trap)**: the schema (`$defs.bug.properties`) stores `id` and `title` as SEPARATE fields. Measure the `title` field WITHOUT the `BUG-1NN: ` prefix, and NOT off the filename (filenames carry slugs like `BUG-107-validate-citations-100pct-noop-reranker-unbound.md`). Measuring off the H1 or filename manufactures a phantom overflow.
   - Independently re-derive the severity census from the 16 files on disk (NOT the self-report). Confirm **94 bug files total** (BUG-024..117) and `next-bug-id.txt` = `BUG-118`.
   - Registry-wide severity re-count by grepping all 94 files: compare to the claimed **1 BLOCKER / 8 CRITICAL / 36 MAJOR / 43 MINOR / 6 COSMETIC = 94**.
   - **4 UPDATEs coherence (BUG-042, 047, 048, 051)**: each got a real update line in `e9425f9` (2 lines each). Confirm each update is coherent (a documented cross-reference/observation, NOT a silent severity flip without rationale). BUG-042 is a breadcrumb dead-`/documents`-link recurrence — confirm the update notes the Phase-6 re-sighting rather than re-minting a new ID.
   - `session-log.md` contains the Phase-6 phase-transition entries: **OPEN** (`[2026-07-11T15:30:06-03:00] … Phase 6 (Observability Hunt, US6) OPEN`) and **CLOSED** (`[2026-07-11T20:26:00-03:00] … Phase 6 … CLOSED → Phase 7 ready`), plus per-scenario verdicts **P6-S1..S4**. NOTE: session-log lines are multi-thousand-char single lines — a token/line-capped read terminates early while appearing complete (METHODOLOGY TRAP). Verify against `tail` / explicit line numbers (the OPEN entry is ~line 213, CLOSED ~line 220), not a truncated read.
   - Coverage: all **4** scenarios exercised, each with an explicit verdict (all FAIL). Confirm the 4-scenario framing is coherent with US6.

8. **SC-006 invariant (zero production code)** — run `git fetch origin develop:develop` FIRST, then diff against `origin/develop`, NOT local `develop` (verify-3's SUGGESTION-2: a stale local `develop` ref is a false positive here). Expected: **empty diff** over `backend/ frontend/ ingestion-worker/ Makefile embedinator.sh embedinator.ps1 'docker-compose*.yml'`. The malformed-PDF ingest test (throwaway collection `audit-timing-test`, failed doc `31503bd4`) is application DATA and does NOT appear in a git diff — it is NOT a production-code edit. Confirm `origin/develop` is an ancestor of HEAD.

9. **R-005-A write boundary (bug-registrar sole writer)** — CRITICAL THIS PHASE given the Lead-solo method. Every Phase-6 new-bug-minting event in `session-log.md` must be tagged `BugRegistrar | discovery`, never `Orchestrator | observation … REGISTERED`. **The Lead drove the browser directly this phase (chrome-devtools MCP) and frontend-inspector was PASSIVE-ONLY** — confirm the solo-drive did NOT cause the Lead/Orchestrator to mint bugs directly; the registrar must remain the sole writer for all 16. **Known false-positive trap**: a case-insensitive `REGISTERED` grep matches "not registered" / "before registration" in deferral/narrative lines — read surrounding context; those are pre-registration narrative, not minting events. Also confirm the Phase-6 commit(s) touch ONLY files under `docs/E2E/2026-05-28-round-1-bug-hunt/` (`git show --name-only e9425f9`).

10. Structural checks: **R-001-A** (checkpoint fields present, prior 5 phases preserved), **R-003-B** (BUG-102..117 contiguous, no gaps/dupes; `next-bug-id.txt`=BUG-118 = 117+1), **R-004-A/B** (`docs/E2E/2026-05-28-round-1-bug-hunt/process/lead-prompt.md` ≤300 lines [is 257], `grep -ci speckit`=0 [is 0]), **dormant-dir guard** (`git log --oneline a5e0886..HEAD -- "docs/E2E/2026-05-15-round-1-bug-hunt/"` must be EMPTY — scope it to `a5e0886..HEAD`; an unscoped `git log` returns pre-apply-0 historical commits and is a false positive).

11. **SC-012 observability-surface key-leak check (T-014 additional, Phase-6 variant).** Phase 6 is Observability, not Settings/Providers, so the Phase-5 "re-run the plaintext audit after state changed" check does not directly apply. BUT Phase 6 surfaced NEW plaintext-exposure sinks: **BUG-117 (Trace Detail dumps raw `JSON.stringify` at TraceTable.tsx ~:178-223)**, the query-traces table, and BUG-116 (no structured log surface). Confirm whether the Phase-6 findings/session-log checked these sinks for plaintext provider-key leakage (`sk-`, `gAAAAA`, decrypted key material in trace/citation JSON). If the hunt did NOT audit the new observability sinks for SC-012, flag as **SUGGESTION** (SC-012 surface not re-audited for the observability sinks introduced/inspected this phase), NOT CRITICAL. Do not manufacture a blocker.

**KNOWN carry-forwards — do NOT fail the gate on these** (accepted, consistent with the verify-1/2/3/4/5 standard):
- **BUG-055..067 reference schema-valid but non-existent local artifact files** (registered pre-convention; artifacts gitignored per FR-022). Treat a non-null artifact PATH as satisfying the "non-null artifact" check. Flag as SUGGESTION (spec-31 backfill), not CRITICAL. Out of Phase-6 scope.
- **Expected uncommitted working-tree state, NOT drift**: modifications to `.claude/skills/gitnexus/*/SKILL.md` (5 files); untracked `docs/superpowers/`; any post-close `session-log.md` observation line. None belong to the phase-6 close commit — do not read them as SC-006 drift.
- **FR-014/FR-015** (no unfiled CRITICAL/BLOCKER; no untriaged MAJOR): per verify-1 ADJUDICATION-1 and verify-2/3/4/5 precedent, these are **hunt-close (Phase-8)** requirements — `Triage (filled in Phase 8 for MAJOR+)` TBD placeholders remain acceptable at Phase-6 scope. Do not re-litigate.
- **CLEANUP items (carried to Pilot / demo-prep — do NOT action here, do NOT fail the gate)**: NEW this phase — failed doc `31503bd4` in collection `audit-timing-test` (BUG-115 live repro), plus a junk-collection pile (`e2e-workflow-*` / `trace-*` / `stream-*` / `us1-create-*` / `france-*`). Carried from Phase 5 — seeded `openai` provider row (is_active=0, BUG-098 repro; rollback `DELETE FROM providers WHERE name='openai'`), `nag-corpus-bge-m3` (1024-dim, P5-S4), `poison-test-p4s5` Qdrant collection (BUG-087 repro; if purging it MUST be via `collections.py::delete_collection`, NEVER `DELETE /api/documents`). **`nag-corpus-spec28` corpus integrity for Phase 7** — flag as a SUGGESTION for the Pilot to re-confirm before apply-7, not a gate blocker.

**Deliverable:** Produce a verify report with findings graded CRITICAL / WARNING / SUGGESTION, plus a **PASS / FAIL / CONDITIONAL-PASS** verdict on the R-002 gate, a Summary Table (one row per check above), an explicit **BUG-107 adjudication verdict** (by-design | regression | ambiguous, with evidence), and a carry-forwards section (preserve verify-5's carry-forwards, updating any that Phase 6 resolved or extended — notably the **verify-7 HARD GitHub-issue-URL debt** now covers **1 BLOCKER + 8 CRITICAL + 36 MAJOR = 45 bugs**, up from 39 at verify-5). Save it to engram topic `sdd/single-round-bug-hunt/verify-report-phase-6` (type: `architecture`, project: `the-embedinator`, `capture_prompt: false`). **Then also RETURN a concise Result Contract as your final message** (status, verdict, CRITICAL/WARNING/SUGGESTION counts, the BUG-107 adjudication one-liner, the verify-report topic key) — do NOT end on an idle notification; the orchestrator gates on the returned summary AND independently re-pulls the artifact.

**Then adjudicate (orchestrator):**
- **PASS or CONDITIONAL-PASS with only WARNING/SUGGESTION** → apply-7 is UNBLOCKED. Record the decision to engram topic `sdd/single-round-bug-hunt/verify-gate-6-decision`.
- **Any CRITICAL** → report the blocker and STOP; do NOT advance to apply-7.

Report back: the verdict, the count of CRITICAL/WARNING/SUGGESTION, the BUG-107 adjudication, and the verify-report topic key.
