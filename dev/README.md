# `dev/` — builder & operational notes (tracked, not public reference)

This directory holds **dev-facing operational material** that belongs in the repo
(it's a visible quality signal) but is **not** part of the public `docs/` reference
surface a new visitor should read first.

Contents:
- `bug-registry-spec26.md` — bug registry from the spec-26 performance-debug pass.

Pointers:
- Public reference docs → `docs/`
- SDD change contracts → `specs/`
- Why operational material lives here rather than in `docs/` →
  `docs/superpowers/specs/2026-06-08-knowledge-system-architecture-design.md` (§7B)
