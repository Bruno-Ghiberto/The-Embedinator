# Agent 7: US5 Wave B — Storage, API, Ingestion, Provider Renames + Test Updates

## Identity
- **Agent type**: `python-expert`
- **Model**: Sonnet 4.6 (`claude-sonnet-4-6`)
- **Wave**: 4 — parallel with A6, runs after Waves 1–3 complete

## Briefing Protocol
**STEP 1**: Read this entire instruction file carefully.
**STEP 2**: Reply to the Orchestrator: "Agent 7 ready. Understood scope: rename log events in storage/API/ingestion/provider modules and update test assertions (US5, T052–T059)."
**STEP 3**: AWAIT the Orchestrator's start signal before beginning any work.
**DO NOT** begin implementation until you receive the explicit start signal.

## Your Scope

Tasks T052–T059. User Story 5 (FR-011, FR-012). Files:
`backend/storage/qdrant_client.py`, `backend/storage/sqlite_db.py`,
`backend/storage/indexing.py`, `backend/ingestion/pipeline.py`,
`backend/api/chat.py`, `backend/middleware.py`, `backend/providers/registry.py`,
and test files with assertions on renamed events.

**Context**: These files were modified in earlier waves. `middleware.py` was updated by A1
(logger fix) and A2 (bind_contextvars). `chat.py` was updated by A1 and A2. `pipeline.py`
was updated by A1 and A2. Your changes are log event string renames only — do not touch
the contextvars logic added by A2.

## Prefix Convention

- `circuit_` — CB events in `qdrant_client.py`
- `storage_` — connection/CRUD events in `qdrant_client.py`, `sqlite_db.py`, `indexing.py`
- `ingestion_` — all events in `pipeline.py`
- `http_` — all events in `chat.py` and `middleware.py`
- `provider_` — all events in `registry.py`
- Add `error=type(exc).__name__` to ALL exception-related `log.error()` and `log.warning()` calls (FR-012)

## MANDATORY: Run gitnexus impact before each file

```python
mcp__gitnexus__impact(target="<SymbolName>", direction="upstream")
# Report to Orchestrator if HIGH or CRITICAL before proceeding
```

## Tasks by File

### T052 — `backend/storage/qdrant_client.py`
CB events → `circuit_` prefix:
- `"qdrant_circuit_half_open"` → `"circuit_qdrant_half_open"`
- `"qdrant_circuit_open"` → `"circuit_qdrant_open"`
Connection/CRUD events → `storage_` prefix:
- `"qdrant_connected"` → `"storage_qdrant_connected"`
Add `error=type(exc).__name__` to exception log calls.

### T053 — `backend/storage/sqlite_db.py`
- `"sqlite_connected"` → `"storage_sqlite_connected"`
Any other events → `storage_` prefix.

### T054 — `backend/storage/indexing.py`
- `"no_chunks_to_index"` → `"storage_no_chunks_to_index"`
- `"chunks_indexed"` → `"storage_chunks_indexed"`

### T055 — `backend/ingestion/pipeline.py`
Rename ALL ~12 events to `ingestion_` prefix:
- `"worker_invalid_json_line"` → `"ingestion_worker_invalid_json_line"`
- `"job_paused_qdrant_outage"` → `"ingestion_job_paused_qdrant_outage"`
Add `error=type(exc).__name__` to error-level log calls.
Do NOT touch the `bind_contextvars` / `clear_contextvars` calls added by A2.

### T056 — `backend/api/chat.py`
- `"query_trace_write_failed"` → `"http_query_trace_write_failed"`
- `"circuit_open_during_chat"` → `"http_circuit_open_during_chat"`
Add `error` fields where applicable.
Do NOT touch the `bind_contextvars(session_id=...)` call added by A2.

### T057 — `backend/middleware.py`
Rename all log events to `http_` prefix.
Do NOT touch the `bind_contextvars(trace_id=...)` / `clear_contextvars()` added by A2.

### T058 — `backend/providers/registry.py`
- `"default_provider_registered"` → `"provider_default_registered"`
- `"unknown_provider_fallback"` → `"provider_unknown_fallback"`
Any other events → `provider_` prefix.

### T059 — Test assertion updates
Search all test files for string assertions on old log event names:
```python
mcp__serena__search_for_pattern(
    pattern=r'(intent_classified|reranker_failed|qdrant_connected|sqlite_connected)',
    relative_path="tests/"
)
```
Update every matching assertion to use the new prefixed name. Be thorough — check
`assert "event_name" in ...`, `== "event_name"`, and log capture assertions.

## Verification

After all files, search for remaining unprefixed events in your files:
- Use `mcp__serena__search_for_pattern` on each modified file
- Confirm every event starts with `http_`, `storage_`, `ingestion_`, `provider_`, or `circuit_`

## MCP Tools to Use
- `mcp__gitnexus__impact` — MANDATORY before each file edit
- `mcp__serena__find_symbol` with `include_body=true` — read current event names
- `mcp__serena__search_for_pattern` — find events and test assertions

## Definition of Done
- [ ] gitnexus impact run and reported for every file
- [ ] `qdrant_client.py`: CB events `circuit_`, connection events `storage_` (T052)
- [ ] `sqlite_db.py`: all events `storage_` prefix (T053)
- [ ] `indexing.py`: all events `storage_` prefix (T054)
- [ ] `pipeline.py`: all events `ingestion_` prefix, contextvars logic untouched (T055)
- [ ] `chat.py`: all events `http_` prefix, contextvars logic untouched (T056)
- [ ] `middleware.py`: all events `http_` prefix, contextvars logic untouched (T057)
- [ ] `registry.py`: all events `provider_` prefix (T058)
- [ ] Test assertions updated for all renamed events (T059)
- [ ] No unprefixed event names remain in any modified file
- [ ] Report to Orchestrator: "A7 complete. T052–T059 done. Test assertions updated."
