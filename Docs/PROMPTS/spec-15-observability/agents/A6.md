# Agent 6: US5 Wave A — Agent + Retrieval Log Event Renames

## Identity
- **Agent type**: `python-expert`
- **Model**: Opus 4.6 (`claude-opus-4-6-20251001`)
- **Wave**: 4 — parallel with A7, runs after Waves 1–3 complete

## Briefing Protocol
**STEP 1**: Read this entire instruction file carefully.
**STEP 2**: Reply to the Orchestrator: "Agent 6 ready. Understood scope: rename all log events in agent and retrieval modules to follow prefix convention (US5, T046–T051)."
**STEP 3**: AWAIT the Orchestrator's start signal before beginning any work.
**DO NOT** begin implementation until you receive the explicit start signal.

## Your Scope

Tasks T046–T051. User Story 5 (FR-011, FR-012). Files:
`backend/agent/nodes.py`, `backend/agent/research_nodes.py`,
`backend/agent/meta_reasoning_nodes.py`, `backend/retrieval/searcher.py`,
`backend/retrieval/reranker.py`.

**Context**: A1 already migrated loggers to `.bind(component=__name__)`. A2 added
`bind_contextvars` calls. Your job is ONLY log event string renames — no logic changes.

## Prefix Convention

- `agent_` — all events in `nodes.py`, `research_nodes.py`, `meta_reasoning_nodes.py`
- `retrieval_` — non-CB events in `searcher.py` and all events in `reranker.py`
- `circuit_` — circuit breaker events in `searcher.py` (regardless of file)
- Replace colons and spaces in event names with underscores
- Add `error=type(exc).__name__` kwarg to ALL `log.warning()` and `log.error()` calls that
  involve caught exceptions (FR-012). Do NOT add to non-exception warnings.

## MANDATORY: Run gitnexus impact before each file

```python
# Before editing each file, run:
mcp__gitnexus__impact(target="<SymbolName>", direction="upstream")
# Report blast radius to the Orchestrator if HIGH or CRITICAL
```

## Tasks by File

### T046 + T047 — `backend/agent/nodes.py`
Read the file with `mcp__serena__get_symbols_overview` (depth=1) then
`mcp__serena__find_symbol` with `include_body=true` for each node function.
Rename ALL ~30 log events to `agent_` prefix. Examples:
- `"init_session_failed"` → `"agent_init_session_failed"`
- `"intent_classified"` → `"agent_intent_classified"`
- `"verify_groundedness: disabled via settings"` → `"agent_verify_groundedness_disabled"`
- `"circuit_open_during_chat"` → `"agent_circuit_open_during_chat"` (stays agent_ since it's in nodes.py; circuit_ prefix is for the CB state machines in searcher.py and qdrant_client.py)

Add `error=type(exc).__name__` to all exception-handling log calls.

### T048 — `backend/agent/research_nodes.py`
Rename ALL ~13 log events to `agent_` prefix. Examples:
- `"orchestrator_no_llm"` → `"agent_orchestrator_no_llm"`
- `"tool_call_failed"` → `"agent_tool_call_failed"`
Add `error=type(exc).__name__` to exception log calls.

### T049 — `backend/agent/meta_reasoning_nodes.py`
Rename ALL ~9 log events to `agent_` prefix. Examples:
- `"alt_queries_generated"` → `"agent_alt_queries_generated"`
- `"reranker_unavailable"` → `"agent_reranker_unavailable"`
Add `error=type(exc).__name__` to exception log calls.

### T050 — `backend/retrieval/searcher.py`
Circuit breaker events → `circuit_` prefix:
- `"circuit_breaker_open"` → `"circuit_searcher_open"`
Search/retrieval events → `retrieval_` prefix:
- `"search_skipped_no_embed_fn"` → `"retrieval_search_skipped_no_embed_fn"`
Add `error=type(exc).__name__` to exception log calls.

### T051 — `backend/retrieval/reranker.py`
All events → `retrieval_` prefix:
- `"reranker_failed"` → `"retrieval_reranker_failed"`
- `"rerank_complete"` → `"retrieval_rerank_complete"`
Add `error=type(exc).__name__` to exception log calls.

## Verification After Each File

After renaming each file, search for any remaining unprefixed events:
```python
mcp__serena__search_for_pattern(pattern=r'log\.(info|warning|error|debug)\("[^a-z]', ...)
```
Confirm no events remain that lack a valid prefix.

## MCP Tools to Use
- `mcp__gitnexus__impact` — MANDATORY before each file edit
- `mcp__serena__find_symbol` with `include_body=true` — read current event names
- `mcp__serena__search_for_pattern` — find all log call sites and verify after rename

## Definition of Done
- [ ] gitnexus impact run and reported for every file
- [ ] ALL events in `nodes.py` use `agent_` prefix (T046+T047)
- [ ] ALL events in `research_nodes.py` use `agent_` prefix (T048)
- [ ] ALL events in `meta_reasoning_nodes.py` use `agent_` prefix (T049)
- [ ] CB events in `searcher.py` use `circuit_` prefix; search events use `retrieval_` (T050)
- [ ] ALL events in `reranker.py` use `retrieval_` prefix (T051)
- [ ] `error=type(exc).__name__` added to all exception-related log calls
- [ ] No unprefixed event names remain in any of these 5 files
- [ ] Report to Orchestrator: "A6 complete. T046–T051 done. N events renamed."
