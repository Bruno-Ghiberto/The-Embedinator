# Agent 8: Phase 8 — Final Validation

## Identity
- **Agent type**: `quality-engineer`
- **Model**: Sonnet 4.6 (`claude-sonnet-4-6`)
- **Wave**: 5 — runs last, after all other agents complete

## Briefing Protocol
**STEP 1**: Read this entire instruction file carefully.
**STEP 2**: Reply to the Orchestrator: "Agent 8 ready. Understood scope: full regression testing and verification of all 7 success criteria (T060–T069)."
**STEP 3**: AWAIT the Orchestrator's start signal before beginning any work.
**DO NOT** begin implementation until you receive the explicit start signal.

## Your Scope

Tasks T060–T069. Phase 8 final validation. No production code changes — verification only.

**Pre-existing failure baseline**: 39 failures. This number must not increase.

## Tasks

### T060 — US5 full test suite (validates Wave 4 renames)

```bash
zsh scripts/run-tests-external.sh -n spec15-us5 tests/
# Poll: zsh scripts/run-tests-external.sh -s spec15-us5
```

Expected: all Wave 4 renames pass with no regressions. If failures appear beyond 39
pre-existing, identify which test and which renamed event caused the failure, then
report to the Orchestrator for targeted fix.

### T061 — Full regression test suite

```bash
zsh scripts/run-tests-external.sh -n spec15-final tests/
# Poll: zsh scripts/run-tests-external.sh -s spec15-final
```

Pre-existing failure count must remain exactly 39.

### T062 — Verify SC-001 (trace_id in all subsystems)

Read the `test_trace_context.py` test results. Confirm tests verify that HTTP requests
produce log entries with `trace_id` from at least 3 subsystems: agent, storage, retrieval.
If the test only covers middleware, flag for supplementation.

### T063 — Verify SC-002 (no trace ID leakage)

Confirm `test_trace_context.py` contains a concurrent isolation test (T014). Review
whether it sends ≥5 concurrent requests. If it only tests 2, note this as a gap.

### T064 — Verify SC-003 (metrics endpoint < 500ms)

Check the `test_metrics.py` for a performance assertion. If none exists (load environment
unavailable), mark as SKIP with note: "Response time assertion not automated; manual
verification required."

### T065 — Verify SC-004 (LOG_LEVEL_OVERRIDES works)

Confirm `test_component_log_levels.py` covers the env var override scenario. Review
T035 test specifically — it must demonstrate that setting an override changes log output
without application restart.

### T066 — Verify SC-005 (StageTimingsChart renders / hidden)

Check that `frontend/components/StageTimingsChart.tsx` exists. Verify `TraceTable.tsx`
has the conditional render guard `stage_timings && Object.keys(stage_timings).length > 0`.
If vitest frontend tests exist for this, check their status.

### T067 — Verify SC-006 (all log events prefixed)

Use `mcp__serena__search_for_pattern` to search all backend `.py` files for
`log.(info|warning|error|debug)\("` patterns. Verify EVERY event string starts with
one of: `http_`, `agent_`, `retrieval_`, `storage_`, `ingestion_`, `provider_`, `circuit_`.
Report any violations found.

### T068 — Verify SC-007 (edge case handling)

Check `test_metrics.py` T025 (empty database returns empty buckets). Check `test_component_log_levels.py`
T036 (invalid override falls back with warning). Check that `StageTimingsChart` handles
`timings` with malformed entries gracefully (does not crash). Report status for each.

### T069 — Write final validation report

Create `specs/015-observability/validation-report.md` with:

```markdown
# Spec 15 Validation Report

**Date**: <today>
**Branch**: 015-observability
**Test suite**: spec15-final
**Total tests run**: <N>
**New tests added**: <N>
**Pre-existing failures**: <N> (baseline: 39)

## Success Criteria

| SC | Description | Status | Notes |
|----|-------------|--------|-------|
| SC-001 | trace_id in all subsystems | PASS/FAIL | |
| SC-002 | No trace leakage between concurrent requests | PASS/FAIL | |
| SC-003 | Metrics endpoint < 500ms | PASS/SKIP | |
| SC-004 | LOG_LEVEL_OVERRIDES works | PASS/FAIL | |
| SC-005 | StageTimingsChart renders / hidden | PASS/FAIL | |
| SC-006 | All log events prefixed | PASS/FAIL | |
| SC-007 | Edge cases handled gracefully | PASS/FAIL | |

## Issues Found
<list any FAIL or gap conditions>
```

## MCP Tools to Use
- `mcp__serena__search_for_pattern` — T067 log event prefix audit
- `mcp__serena__find_symbol` — verify specific implementation details for SC checks

## Definition of Done
- [ ] T060: US5 test suite PASSED, ≤39 pre-existing failures
- [ ] T061: Full suite PASSED, pre-existing failures = 39
- [ ] T062–T068: All 7 SCs verified with PASS or documented SKIP
- [ ] T069: `specs/015-observability/validation-report.md` written
- [ ] Report to Orchestrator: "A8 complete. Spec 15 validation done. See validation-report.md."
