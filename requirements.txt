# The Embedinator — Python Backend Dependencies

# Web framework
# Upper bound is deliberate. 0.135.x is the line the suite is validated against.
# 0.141 changed include_router() to append a fastapi.routing._IncludedRouter
# wrapper instead of flattening routes into app.routes — endpoints still serve,
# but anything introspecting app.routes silently sees nothing. Without a bound,
# CI resolved 0.141 on every fresh install while local venvs stayed on 0.135,
# so the two were testing different code. Raise this only as a deliberate,
# verified upgrade, not as a side effect of an unrelated install.
fastapi>=0.135,<0.136
uvicorn[standard]>=0.46.0
python-multipart>=0.0.18

# Configuration
pydantic>=2.0
pydantic-settings>=2.0

# Database
aiosqlite>=0.22.1

# Vector database
qdrant-client>=1.17.1

# Cross-encoder reranking
sentence-transformers>=5.2.3

# HTTP client (for Ollama/provider API calls)
httpx>=0.28.1

# Logging
structlog>=24.0

# PDF parsing
pypdf>=5.0

# API key encryption
cryptography>=44.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=7.1.0
pytest-httpx>=0.36.2

# LangGraph agent framework
langgraph>=1.0.10
langchain-core>=1.2.10

# LangChain provider adapters (lazy-imported in registry.py)
langchain-ollama>=0.3
langchain-openai>=0.3
langchain-anthropic>=1.4.3

# LangGraph checkpointing
langgraph-checkpoint-sqlite>=2.0

# Token counting (spec-26: FR-007 true token counter for message trimming)
tiktoken>=0.8

# Linting
ruff>=0.15.12

# Type checking and security auditing (CI dev-deps)
# Note: pydantic.mypy plugin auto-activates via pyproject.toml [tool.mypy].plugins
# when pydantic is installed (see line above). No separate `pydantic[mypy]` extra needed
# (deprecated in pydantic 2.x).
mypy>=1.11
pip-audit>=2.10.0
