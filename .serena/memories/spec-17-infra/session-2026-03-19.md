# Spec-17 Infrastructure Setup — Implementation Session (2026-03-19)

## Status: COMPLETE ✅
- Branch: `017-infra-setup` (pushed to origin)
- All 44 tasks (T001–T044) done
- All 8 SCs: PASS
- Test gate: 0 new failures (1487 passing, 39 pre-existing, 87% coverage)
- Commits: `c8fe884` (spec-16), `c5ea44b` (spec-17)

## Agent Teams Wave Structure
- Wave 1 — A1 (devops-architect): T001–T007 — baseline test + 6-file audit
- Wave 2 — A2 (python-expert): T030–T035 — config.py + .env.example (parallel)
- Wave 2 — A3 (python-expert): T019–T021 — Dockerfile.backend rewrite (parallel)
- Wave 3 — A4 (backend-architect): T008–T029, T036–T039 — Makefile + docker-compose
- Wave 4 — A5 (quality-engineer): T040–T044 — polish + final validation

## Files Modified
| File | Change |
|------|--------|
| `Dockerfile.backend` | 2-stage rust:1.93 + python:3.14-slim; non-root appuser; binary at /app/ingestion-worker/target/release/embedinator-worker |
| `backend/config.py` | EMBEDINATOR_FERNET_KEY alias + populate_by_name=True (Constitution V) |
| `.env.example` | All 28 Settings fields; EMBEDINATOR_FERNET_KEY (not API_KEY_ENCRYPTION_SECRET); CONFIDENCE_THRESHOLD=60; DEFAULT_LLM_MODEL=qwen2.5:7b |
| `Makefile` | 15 targets with ## self-doc; up/down renamed from docker-up/docker-down; help target |
| `docker-compose.yml` | UPLOAD_DIR + LOG_LEVEL_OVERRIDES + RUST_WORKER_PATH added; SQLITE_PATH=/data/embedinator.db fixed; GPU passthrough added; ./data:/data volume fixed |
| `docker-compose.prod.yml` | Comment header added |
| `frontend/next.config.ts` | output: "standalone" added (FR-013) |
| `specs/017-infra-setup/validation-report.md` | Per-SC/FR status table |

## Key Decisions / Gotchas
- **docker-compose.dev.yml discrepancy**: File was a dev overlay (backend+frontend hot-reload), not standalone Qdrant+Ollama. A4 chose option (b): `dev-infra` target uses `docker compose up -d qdrant ollama` from base file rather than `-f docker-compose.dev.yml`. File left unchanged.
- **Makefile target count**: Spec says "14" but lists 15 names (including help). 17-implement.md explicitly notes ambiguity. All 15 present with ## comments. SC-005/SC-008 both PASS.
- **FR-006 PARTIAL**: frontend service has no healthcheck (3/4 services covered). Not a blocker — frontend is a leaf service.
- **Constitution V**: Requires both `alias="EMBEDINATOR_FERNET_KEY"` AND `populate_by_name=True` in model_config — without both, pydantic-settings won't read the alias.
- **A2 gate (T035)**: test_config.py — 2 passed, 1 failed (pre-existing test_default_settings) — within threshold.

## Verified OK (No Changes Needed)
- requirements.txt: langchain-core, langchain-ollama, langgraph-checkpoint-sqlite all correct
- .gitignore: all 8 required patterns present
- ingestion-worker/Cargo.toml: all 6 Rust deps present
- frontend/Dockerfile: USER nextjs + .next/standalone already compliant
- docker-compose.dev.yml: left as dev overlay per option (b)
