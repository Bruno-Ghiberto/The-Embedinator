# Spec-16 Testing Strategy — Implementation Session (2026-03-19)

## Status: COMPLETE ✅
- Branch: `016-testing-strategy`
- Tests: 1487 passing, 39 pre-existing failures (unchanged), 0 regressions
- Coverage: 87% (gate at 80%)
- All 8 SCs: PASS

## What Was Built
No production code modified. Test infrastructure only:

### Shared Infrastructure
- `tests/conftest.py` — 4 fixtures: `db` (in-memory SQLiteDB), `sample_chunks` (3 RetrievedChunks), `mock_llm` (BaseChatModel mock), `mock_qdrant_results` (2 raw Qdrant dicts)
- `pytest.ini` (project root) — `asyncio_mode=auto`, `--cov-fail-under=80`, markers registered

### Wave 2 — Unit Tests (A2+A3, 56 tests total)
- `tests/unit/test_reranker.py` — 11 tests, mock CrossEncoder via patch
- `tests/unit/test_score_normalizer.py` — 14 tests, function `normalize_scores`, uses `chunk.dense_score`
- `tests/unit/test_storage_chunker.py` — 18 tests, function `chunk_text`
- `tests/unit/test_storage_indexing.py` — 6 tests, async `index_chunks`, mocked app.state
- `tests/unit/test_errors.py` — 7 tests, 11 classes from `backend.errors` only

### Wave 3 — E2E + Integration (A4+A5, 26 tests total)
- `tests/e2e/test_ingest_e2e.py` — 2 tests
- `tests/e2e/test_chat_e2e.py` — 4 tests, NDJSON stream event verification
- `tests/e2e/test_collection_e2e.py` — 4 tests, full CRUD lifecycle
- `tests/e2e/test_observability_e2e.py` — 5 tests, traces+metrics endpoints
- `tests/integration/test_qdrant_integration.py` — 4 tests
- `tests/integration/test_hybrid_search.py` — 4 tests
- `tests/integration/test_circuit_breaker.py` — 3 tests

### Wave 4 — Fixtures (A6)
- `tests/fixtures/sample.pdf` — 2104 bytes, valid PDF (%PDF magic bytes)
- `tests/fixtures/sample.md` — headings, list, code block, prose
- `tests/fixtures/sample.txt` — 605 words plain UTF-8

## Critical Technical Gotchas

### httpx E2E Transport Pattern
```python
# CORRECT (httpx >=0.28):
async with httpx.AsyncClient(transport=httpx.ASGITransport(app=app), base_url="http://test") as c:
    yield c
# WRONG — app= kwarg removed in newer httpx:
async with httpx.AsyncClient(app=app, base_url="http://test") as c:
```

### QdrantStorage API (not obvious from docs)
- `batch_upsert()` requires `QdrantPoint` objects, NOT plain dicts
- `collection_exists(name)` — NOT `list_collections()`

### E2E Collection Name Validation
- POST /api/collections with invalid name → returns **422** (Pydantic validates first) not 400
- Assert `status_code in (400, 422)` not `== 400`

### spec16-final.status = FAILED (expected)
- pytest exits non-zero whenever there are ANY failures
- 39 pre-existing failures → status always FAILED
- Check by counting: `grep "FAILED" Docs/Tests/spec16-final.log | wc -l` must equal 39

### run-tests-external.sh ONE target rule
- Script accepts ONE positional target (last arg wins)
- For multi-file gates: run separate invocations per file
- DO NOT: `zsh scripts/run-tests-external.sh -n foo file1.py file2.py` (file2.py wins)

### Pre-existing Failures Baseline
- Baseline: 33 failed + 6 errors = 39 total non-standard outcomes
- All in test_schema_migration.py (19) + test_config.py (1) + integration/app_startup etc.
