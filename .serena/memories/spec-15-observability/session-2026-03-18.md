# Spec 15: Observability — Session 2026-03-18

## Status: DESIGN COMPLETE (specify → clarify → plan → tasks)

### Pipeline Completed
1. **15-specify.md** rewritten by technical-writer agent — 15 inconsistencies fixed (wrong spec refs, wrong class names, SSE→NDJSON, missing query_traces columns, etc.)
2. **spec.md** generated via /speckit.specify — 5 user stories, 14 FRs, 7 SCs, 0 clarifications needed initially
3. **Clarifications** (2 questions): FR-011 = full codebase log event rename; FR-014 = background ingestion trace propagation in-scope
4. **15-plan.md** rewritten by technical-writer agent (844 lines) — 5 waves, 8 agents, 62 tasks in the plan
5. **plan.md** generated via /speckit.plan — constitution check PASS (all 8 principles)
6. **research.md** — 5 research decisions (R1-R5) resolved
7. **data-model.md** — 4 new entities (all computed/ephemeral, no schema changes)
8. **contracts/metrics-api.md** — GET /api/metrics full API contract
9. **tasks.md** generated via /speckit.tasks — 69 tasks (T001-T069), 8 phases

### Key Design Decisions
- R1: Per-component log levels via custom structlog processor with `DropEvent` (PrintLoggerFactory bypasses standard logging)
- R2: Fixed window→bucket mapping: 1h→5m, 24h→1h, 7d→1d
- R3: Circuit breaker state via direct `getattr()` on `app.state`
- R4: Static analysis pre/post rename for log event audit
- R5: New UUID per `ingest_file()` call (not inherited from HTTP request)

### Scope
- 14 FRs (FR-001 to FR-014), 7 SCs, 5 user stories
- ~17 files modified (14 backend + 3 frontend), 2 new frontend files, 4 new test files
- No new dependencies, no schema migrations
- FR-011: FULL codebase-wide log event rename (~90+ events across 12 modules)
- FR-014: Background ingestion trace ID generation

### Agent Teams (from 15-plan.md)
- Wave 1: A1 quality-engineer (Opus) — audit
- Wave 2: A2 + A3 python-expert (Sonnet) — trace propagation + metrics endpoint
- Wave 3: A4 python-expert + A5 frontend-architect (Sonnet) — log levels + stage timings viz
- Wave 4: A6 python-expert (Opus) + A7 python-expert (Sonnet) — log event renames
- Wave 5: A8 quality-engineer (Sonnet) — final validation

### Ready For
- /speckit.analyze or /speckit.implement
