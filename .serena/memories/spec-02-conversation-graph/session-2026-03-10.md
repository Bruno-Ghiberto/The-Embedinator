# Spec 02: ConversationGraph — Session 2026-03-10

## Status
- **Spec**: Complete and clarified (`specs/002-conversation-graph/spec.md`)
- **Plan context prompt**: Improved and coherent (`Docs/PROMPTS/spec-02-conversation-graph/02-plan.md`)
- **Branch**: `002-conversation-graph`
- **Next step**: `/speckit.plan` to generate implementation plan from the context prompt

## Clarification Decisions (from /speckit.clarify)
1. Collection management handler is OUT OF SCOPE — route only, stub node
2. Max 2 clarification rounds per query, then best-effort as rag_query
3. History compression triggers at 75% of configured model's context window
4. No collections selected → prompt user to select at least one

## Key Fixes Applied to 02-plan.md
- All SSE references → NDJSON (ADR-007)
- Streaming pattern: `stream(stream_mode="messages")` not `astream_events`
- Interrupt pattern: `interrupt()` + `Command(resume=...)` (modern LangGraph API)
- Added missing `summarize_history` node
- Added `intent` field extension to ConversationState
- Added Agent Team Strategy (5 waves with worktree isolation)
- Added Existing Phase 1 Modules table (reuse, don't rebuild)
- Added Node Error Handling table
- Phase 2 stubs explicitly documented (verify_groundedness, validate_citations)

## Files Modified This Session
- `specs/002-conversation-graph/spec.md` — clarifications integrated (FR-001, FR-009, FR-011, FR-013, US3-S4, US4-S4, edge cases)
- `specs/002-conversation-graph/checklists/requirements.md` — all items pass
- `Docs/PROMPTS/spec-02-conversation-graph/02-plan.md` — full rewrite for coherence
