# The Embedinator — Session 2026-03-13

## Goal
Implement Spec-05 (Accuracy, Precision & Robustness Enhancements) — 4-wave Agent Teams orchestration.

## Instructions
- Always start sessions with `/sc:load engram and serena`
- Agent Teams is MANDATORY for implementation when implement.md specifies it
- NEVER run pytest inside Claude Code — use `scripts/run-tests-external.sh`

## What Was Done

### Implementation (4 waves, 7 agents, 49 tasks)
- Wave 1 (A1/Sonnet): Foundation — VERIFY_PROMPT, test skeletons, settings verification
- Wave 2 parallel (A2/Opus + A3/Sonnet + A4/Sonnet): GAV node / citation validation / TIER_PARAMS
- Wave 3 (A5/Opus + A6/Sonnet → A7/Sonnet): Circuit breakers / NDJSON / integration tests
- Wave 4 (A1/Sonnet): Polish + full regression

### Results
- **335 tests passing**, 5 pre-existing failures, 0 regressions
- Spec-05 implementation complete on branch `005-accuracy-robustness`

## Key Decisions / Gotchas
- Integration tests: `MemorySaver()` not `AsyncMock()` for LangGraph checkpointer (strict type validation)
- Tenacity retry on inner `_*_with_retry` methods; CB recording only in outer public method after retries exhausted
- Wave 2 parallel agents use local imports in tests to avoid module-level lazy-import interference
- `import time` needed at nodes.py module level for inference CB globals
- CB half-open: after cooldown, set `_circuit_open = False` to allow one probe request through

## Next Steps
- Spec-06 (Ingestion pipeline) — will create `backend/ingestion/embedder.py` (US5 deferred from spec-05)
