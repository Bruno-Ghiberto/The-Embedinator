# Spec 06: Ingestion Pipeline — Session 2026-03-13

## Status
- Speckit workflow: specify ✅ → clarify ✅ → plan ✅ → tasks ✅ → analyze ✅ → implement context ✅
- Next step: `/speckit.implement` (5-wave Agent Teams execution)

## Key Decisions
- 8 agents across 5 waves: A1 (foundation), A2-A5 (parallel core), A6-A7 (parallel dedup+validation), A8 (fault tolerance), Lead (polish)
- A2 (Pipeline Orchestrator) uses Opus 4.6 — most complex component; all others use Sonnet 4.6
- Agent types: A1=backend-architect, A2=python-expert, A3=python-expert, A4=performance-engineer, A5=system-architect, A6=python-expert, A7=security-engineer, A8=quality-engineer
- Config: only 3 truly new fields (rust_worker_path, embed_max_workers, qdrant_upsert_batch_size); 5 already exist in config.py

## Codebase Facts (Serena-Verified)
- config.py already has: upload_dir, max_upload_size_mb, parent_chunk_size, child_chunk_size, embed_batch_size
- ParentStore.get_by_ids() uses SQL column aliases (parent_id, collection) that must match the Pydantic model — update SQL, NOT the model
- QdrantClientWrapper (not QdrantStorage) has full circuit breaker with half-open + cooldown
- IngestionError and CircuitOpenError already exist in errors.py
- backend/ingestion/ and ingestion-worker/ do NOT exist yet
- Phase 1 documents table uses collection_ids JSON array — needs migration to collection_id TEXT

## Analyze Findings
- 0 CRITICAL, 2 HIGH (config field gap — turned out to be false positive after Serena verification), 6 MEDIUM, 2 LOW
- 100% FR coverage (20/20 FRs have tasks)
- Constitution: 7/7 PASS, 2 documented deviations (UUID5 formula, MIME scope) both justified

## Files Produced
- 06-implement.md: 1,143 lines (rewrite from 416)
- 8 agent instruction files in agents/ directory (A1-A8)
